import SearchBar from './SearchBar/SearchBar';
import { Component } from 'react';

export App () extends Component {
  
handleSubmit = await axios.get 



  render(){
    return (
    <div>
      <SearchBar onSubmit={this.handleSubmit} />
    </div>
  );
    }
};
